/*import React, { useEffect } from 'react'
import UserProfileForm from './UserProfileForm'
import { getUsersData } from '../../data/user'
const Profile = () => {
  const [data, setData] = useData(null)

  useEffect(() => {
    const fetchData = async () => {
      setData(await getUsersData)
    }
    fetchData()
  }, [])

  return data ? <UserProfileForm preloadedValues={data} /> : <div>Patientez un instant...</div>
}

export default Profile;
*/
